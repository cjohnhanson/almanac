fresh-eyes: agent review of the suite race fix. Scope: the curated-verified scratch files move from shared /tmp names into the path's own work dir. An independent reviewer confirmed in source that status and sync --check read only the manifest and skills/, that dev:$PWD parses the same as the old absolute path, and that the two parallel paths now share no filename, which removes the collision structurally. Two consecutive full-suite nix-backend runs pass as regression evidence. Mutation: the pre-fix state is the mutant — with the shared /tmp names, a run failed on 2026-08-17 at the restore assertion.
